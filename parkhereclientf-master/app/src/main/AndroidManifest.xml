<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.imwageesha.made">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/frlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration" />
        <activity android:name=".Profile" />
        <activity android:name=".fgtpw" />
        <activity
            android:name=".Main2Activity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".VehicleEditor"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".NavBar"
            android:label="@string/title_activity_nav_bar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Payment" />
        <activity android:name=".History" />
        <activity android:name=".Promotions" />
        <activity android:name=".MyPoints" />
        <activity android:name=".Help" />
        <activity android:name=".Settings" />
        <activity android:name=".PreBookDetails" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key_new" />

        <activity
            android:name=".PreMap"
            android:label="@string/title_activity_pre_map" />
        <activity
            android:name=".Temp"
            android:label="@string/title_activity_temp" />
        <activity
            android:name=".About"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NewProf"
            android:label="@string/title_activity_new_prof"
            android:theme="@style/AppTheme" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".Datentime" />
        <activity
            android:name=".Direction"
            android:label="@string/title_activity_direction" />
        <activity android:name=".BookingHistoryOptions"></activity>
    </application>

</manifest>